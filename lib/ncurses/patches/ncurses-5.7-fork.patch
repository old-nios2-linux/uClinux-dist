Index: ncurses-5.7/test/ditto.c
===================================================================
--- ncurses-5.7/test/ditto.c	(revision 9061)
+++ ncurses-5.7/test/ditto.c	(working copy)
@@ -162,7 +162,11 @@
 	failed(slave_name);
     }
     sprintf(s_option, "-S%s/%d", slave_name, aslave);
+#ifdef __uClinux__
+    if (vfork()) {
+#else
     if (fork()) {
+#endif
 	execlp("xterm", "xterm", s_option, "-title", path, (char *) 0);
 	_exit(0);
     }
